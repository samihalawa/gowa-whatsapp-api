interface:
  display_name: "GOWA WhatsApp API"
  short_description: "Use direct curl calls to GOWA for WhatsApp chats, messages, groups, and outreach"
  default_prompt: "Use $gowa-whatsapp-api for WhatsApp automation via gowa.megawebs.com. Always call the GOWA REST API directly with curl through the cors.trigox.workers.dev proxy, never the WhatsApp Go MCP tools."
